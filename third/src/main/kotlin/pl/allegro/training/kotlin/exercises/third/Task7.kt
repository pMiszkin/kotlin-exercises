package pl.allegro.training.kotlin.exercises.third

/**
 * Implement all necessary functions to make it possible to have a for loop
 * over a pair as in [pairContains].
 *
 * Handy information:
 * * a `for` loop requires an object to have an `iterator()` operator function which returns an object which should have:
 * *** an operator `hasNext(): Boolean` function
 * *** an operator `next(): T` function
 */

fun pairContains(pair: Pair<String, String>, word: String): Boolean {
    TODO()
    // After the implementation replace TODO() with the body of a function:
//    for (value in pair) {
//        if (value == word) return true
//    }
//
//    return false
}
