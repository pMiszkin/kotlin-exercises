package pl.allegro.training.kotlin.exercises.third

import org.jetbrains.spek.api.Spek
import org.jetbrains.spek.api.dsl.describe
import org.jetbrains.spek.api.dsl.it
import org.jetbrains.spek.api.dsl.on
import org.junit.Assert.assertEquals

/*
object Task1Spek: Spek({
    describe("lastChar extension function") {

        on("lastChar on Hello") {
            val ch = "Hello".lastChar

            it("should return o") {
                assertEquals('o', ch)
            }
        }

        on("lastChar on empty string") {
            val ch = "".lastChar

            it("should return null") {
                assertEquals(null, ch)
            }
        }

        on("lastChar on null string") {
            val ch = (null as String?).lastChar

            it("should return null") {
                assertEquals(null, ch)
            }
        }
    }
})
*/