package pl.allegro.training.kotlin.exercises.third

/**
 * Implement an extension function containsTimes(word: String, times: Int) on `List<String>` which returns _true_
 * if the list contains `word` `times` times.
 */

fun regularContainsTimes(list: List<String>, word: String, times: Int): Boolean =
        TODO()
// Replace the TODO() with the line below after you implement the extension function
//        list.containsTimes(word, times)
